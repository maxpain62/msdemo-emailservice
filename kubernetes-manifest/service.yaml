apiVersion: v1
kind: Service
metadata:
  name: msdemo-adservice-service
  labels:
    app: msdemo-adservice
spec:
  selector:
    app: msdemo-adservice
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  type: ClusterIP