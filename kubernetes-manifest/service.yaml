apiVersion: v1
kind: Service
metadata:
  name: msdemo-emailservice-service
  labels:
    app: msdemo-emailservice
spec:
  selector:
    app: msdemo-emailservice
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  type: ClusterIP